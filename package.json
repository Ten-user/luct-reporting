{
  "name": "luct-reporting-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run install-all",
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npx concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}